### SimpleFactory

- A factory class that decides which concrete class to implement.

### FactoryMethod

- Defines an interface(abstract class) for creating Objects but allows subclass to decide which classes to instantiate.
- The concrete factory classes will decide which products to instantiate.

### AbstractFactory

- Provides an interface for creating families fo related objects without specifiying their concrete classes.
- One factory can have multiple type of products
